# Local dev secrets via 1Password references (safe to commit — references, not values).
# Run the app with:  op run --env-file=.env.op -- npm run dev
# CI/prod uses .env.template + op inject in the deploy workflow instead.
DATABASE_URL="op://freight-auditor-dev/Database/connection_string"
SESSION_SECRET="op://freight-auditor-dev/App/session_secret"
